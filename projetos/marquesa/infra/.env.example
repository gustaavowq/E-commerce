# =============================================================================
# Marquesa — env do docker-compose (substituicao ${VAR})
# Copiar pra infra/.env na VPS. NAO comitar.
#
# DB_PASSWORD precisa BATER com a senha que aparece em
# backend/.env.production no DATABASE_URL=postgresql://marquesa:SENHA@db:...
# =============================================================================

DB_PASSWORD=CHANGE_ME_USE_A_MESMA_SENHA_DO_DATABASE_URL
